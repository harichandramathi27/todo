version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install 
  build:
      commands:
        - npm run build
        -ls -la dist
        -ls -la dist/assests
artifacts:
  base-directory: dist
  files:
    - '**/*'
