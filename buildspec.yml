version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo installing dependencies...
      - npm install --legacy-peer-deps
  build:
      commands:
        - echo Building the reactapp...
        - npm run build
artifacts:
  files:
    - '**/*'
  base-directory: dist
  discard-paths: no
